/// The gallery's interactive playground.
///
/// The pieces live in `playground/`: the state bag every control drives
/// (`playground_state.dart`), the shareable-link codec (`share_codec.dart`),
/// the snippet generator (`snippet.dart`), the control widgets
/// (`controls.dart`), and the section itself (`playground_section.dart`).
library;

export 'playground/playground_section.dart' show PlaygroundSection;
